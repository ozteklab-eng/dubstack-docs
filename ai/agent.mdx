---
title: Agent
description: Use the dashboard agent to answer, draft, and maintain documentation with connected repository and integration context.
icon: sparkles
---

The dashboard agent helps trusted workspace members maintain documentation from the dashboard and connected collaboration tools.

It is separate from the public assistant. The public assistant answers reader questions from published docs. The dashboard agent can use private deployment context and, when configured, create documentation changes through the connected repository.

## What the agent can do

- Answer questions about the selected deployment.
- Inspect published docs and managed docs indexes.
- Use connected integration context such as Slack or Jira when enabled.
- Draft documentation changes.
- Open pull requests through an authoring-capable Git provider.
- Support automations that run on schedules, code changes, or content updates.

## Requirements

Agent authoring requires:

- a selected deployment
- a connected docs repository
- authoring permissions for the Git provider
- an authoring model profile
- connected integrations when the request needs external context

The dashboard shows readiness checks for the docs index, managed index, advanced answers, Slack, and repository access.

## Agent page sections

The Agent page changes based on what is connected for the selected deployment.

| Section | What to check |
| --- | --- |
| Agent settings | Shows whether the Slack app is connected and which workspace it is connected to. |
| Enabled integrations | Shows connected providers that can supply context to the agent. |
| Available to your team | Lists provider cards that are not connected yet. Cards marked as coming soon are catalog entries, not active context sources. |
| Repository access | Shows whether the docs repository can be used for authoring and links to Git settings. |
| Agent readiness | Shows the published docs index, managed docs index, and advanced answer path used by the agent. |

## Usage reporting

Agent runs can record usage metadata such as source, run type, profile, model route, connector context, status, token counts, and credit estimates.

See [AI credits and usage](/ai/credits-usage) for how usage is tracked across dashboard, Slack, assistant, and workflow surfaces.

## Repository access

The agent writes through Git.

For GitHub-backed deployments, install the GitHub app on the repository and grant pull request permissions.

For GitLab-backed deployments, connect GitLab authoring access from Git settings before running workflows that create merge requests.

## Integration context

Connected integrations can provide scoped context to the agent:

| Integration | Common use |
| --- | --- |
| Slack | Read bounded thread or channel context and continue authoring conversations. |
| Jira | Read selected issue metadata from a bounded JQL query. |

Only connected integrations that support the requested action are made available to the agent.

Provider cards that are visible but marked as coming soon cannot be selected for agent runs or automations yet. Use connected providers only when testing or documenting a production workflow.

## Readiness checks

Use **Sync index** when the published docs changed and the managed retrieval index should be refreshed. Use **Refresh** when you only need to re-check the current readiness state.

The readiness panel separates three paths:

| Path | Meaning |
| --- | --- |
| Published docs | The fast path backed by the published docs snapshot. |
| Managed docs index | The managed retrieval index built for deeper answers. |
| Advanced answers | The answer profile, retrieval route, fallback route, and prompt configuration for the selected site. |

If any path is not ready, fix the blocking repository, indexing, or profile issue before asking the agent to draft documentation changes.

## Managed deployments

Some managed deployments stay readable but disable authoring controls to prevent accidental repository changes.

Create a disposable deployment when testing Slack, Git, or automation authoring.

## Safety boundaries

- The agent must stay scoped to the selected organization and deployment.
- Public reader assistant requests do not grant dashboard authoring access.
- Repository changes should use pull request review unless the workflow is intentionally configured for direct updates.
- Secrets and private tokens should not be committed to documentation.

<Callout type="info">
  Use [Automations](/automations) when you want the agent to run from a recurring trigger or repository event.
</Callout>

---
title: Workflow integrations
description: Use connected integrations as bounded context and notification channels for workflow runs.
icon: plug-zap
---

Workflow integrations let a workflow use selected external context while it drafts documentation updates.

Use integrations when the source of truth for a docs update lives outside the docs repository, such as a Slack thread, Jira issue, release discussion, or support handoff.

<Callout type="info">
  Integrations provide context or notifications. They do not become the documentation source of truth. Workflow changes still write through the connected Git provider.
</Callout>

<Callout type="info">
  Workflow integrations require Enterprise access or an active trial that includes integration features.
</Callout>

## Requirements

Before a workflow can use an integration, confirm:

- the integration is connected from **Settings > Organization > Integrations**
- the current deployment is eligible for workflows and integration features
- the workflow supports integration context
- the workflow configuration selects the integration
- the connected account has access to the source data
- repository authoring is ready for any workflow that creates docs changes

If any requirement is missing, the workflow should show a blocking reason instead of running with incomplete context.

## Supported integration context

| Integration | Workflow use | Required setup |
| --- | --- | --- |
| Slack | Read selected channel or thread context and send workflow notifications. | Connect Slack, invite the app to private channels if needed, and select the channel or thread in workflow settings. |
| Jira | Read bounded issue context from a JQL query. | Connect Jira and configure a JQL query plus an issue limit. |
| Confluence | Read bounded page context from a CQL query. | Connect Confluence and configure a CQL query plus a page limit. |

Other providers can appear as catalog items before they are available. Catalog-only providers cannot be selected for a run until authorization, scopes, collectors, access checks, and workflow support are active.

Jira JQL must be bounded. Use a project, issue key, label, fix version, status, or updated-date window. Queries that only sort results, such as `ORDER BY updated DESC`, are rejected safely instead of scanning the whole workspace.

Confluence CQL must also be bounded. Use a content type, space, label, title, owner, parent, or date window. Queries that only sort results, such as `ORDER BY lastmodified DESC`, are rejected safely instead of scanning the whole workspace.

Support-feedback providers are currently catalog-gated for workflows. The **Update from support feedback** workflow remains visible, but it cannot run until a supported support-feedback connector, collector, and readiness check are available for the deployment.

## Shared and personal integrations

Workflow integrations can be shared or personal.

| Scope | Behavior |
| --- | --- |
| Shared | One organization-level connection is available to permitted workflows and workspace members. Use this for team systems such as Slack, Jira, issue trackers, knowledge bases, support tools, and CRM systems. |
| Personal | Each user authorizes their own account. Use this for user-specific context such as calendar or drive data when that context should not be shared across the whole organization. |

Current shipped workflow integrations are shared. Personal integrations are reserved for providers where the data naturally belongs to one user.

## Select integrations for a workflow

1. Open **Automate > Workflows**.
2. Choose a workflow.
3. Open the configuration drawer.
4. Find the integration context section.
5. Select the connected integrations the workflow should use.
6. Fill in provider-specific fields, such as Slack channel, Slack thread timestamp, Jira JQL, Confluence CQL, or context limits.
7. Save the workflow.

Only integrations that are connected and supported by that workflow appear as selectable options.

## Source repositories and code-change triggers

Some workflows can use source repositories as read-only context.

Source repositories are product or code repositories. They are not the docs
repository unless you deliberately select the same repository for both jobs.

| Repository role | What it does |
| --- | --- |
| Docs repository | Publishes the docs site and receives generated documentation changes. |
| Source repository | Provides code, product, API, or changelog context to a workflow. |
| Copy target | Receives an explicit repository copy for migration or backup testing. |

For **Update from code changes**, a selected source repository has two roles:

- scheduled runs can inspect recent repository activity as bounded context
- code-change runs can be queued from validated merged pull request or merge request events

The source repository does not become the docs repository. Generated changes still target the connected docs source and use the workflow update mode you selected.

If a source provider webhook is unavailable or pending, use a scheduled run or run the workflow manually after the source change has merged.

In Git settings, source repository cards show the selected scope, supported event types, webhook status, and the last configuration time. Use those fields to confirm that the repository is connected for the workflow you expect before debugging the workflow runner.

<Callout type="info">
  Source repository events queue eligible workflow runs. They do not publish pages directly. The generated docs update must still be committed to, or merged into, the active docs source.
</Callout>

Copy targets can also be used by the **Repository copy targets** workflow. That workflow is scheduled or manual only; it does not create a continuous mirror. Use it when you want a recurring backup or a migration rehearsal without switching the active deployment source.

### Provider behavior

Source repository support follows the provider connection that owns the repository.

| Provider | Source repository behavior |
| --- | --- |
| GitHub | Uses the GitHub App installation to read selected repositories and receive pull request events. |
| GitLab | Uses the GitLab project connection and OAuth authoring access when merge request context or merge-request authoring is required. |
| Forgejo | Uses the configured Forgejo instance, repository, branch, and token. When the source webhook is active, merged review events can queue code-change runs. If the webhook is pending, scheduled and manual runs can still use the repository as context. |

For self-hosted providers, the instance must be reachable from dubstack over HTTPS for API reads and webhook delivery. If inbound webhooks are blocked by your network, keep the repository selected for scheduled or manual workflow runs and trigger the workflow after the source change is merged.

### Add a code repository as context

Use this flow when your docs live in one repository and your product code lives in another.

1. Open **Settings > Deployment > Git settings**.
2. Confirm the active deployment source points to the docs repository.
3. Add or select the code repository as a workflow source repository.
4. Choose the provider, repository, and branch.
5. Save and confirm the source repository card validates successfully.
6. Open **Automate > Workflows**.
7. Configure **Update from code changes**.
8. Select the source repository in the workflow's context section.
9. Add a path scope in the workflow configuration if the source repository is broad.
10. Keep **Require review** enabled until the workflow output is trusted.

After setup, merged review events can queue code-change runs where the provider integration supports them. Scheduled and manual runs can still read the selected repository as bounded context.

### Narrow source context

When a source repository is broad, add a path scope in the workflow configuration.

Use a relative path such as `apps/web`, `services/api`, or `packages/sdk`. This keeps the workflow focused on the service, package, or app that should influence the draft.

Path scope is most useful for monorepos and platform repositories where unrelated code changes would add noise to the run.

## Keep context bounded

Use the narrowest context that can answer the workflow's task.

Good examples:

```text
Use Slack thread 1719400000.000000 from #release-notes and draft a changelog entry.
```

```text
Use Jira context with jql: project = DOCS AND fixVersion = "2026.07" ORDER BY updated DESC, limit 5.
```

```text
Use Confluence context with cql: type = page AND space = DOC ORDER BY lastmodified DESC, limit 5.
```

```text
Use the source repository context under services/api and update the API reference only when public endpoint behavior changed.
```

Avoid broad context:

```text
Read everything in Slack and update the docs.
```

Broad context increases cost, makes the output harder to review, and can include irrelevant source material.

## Notifications

Slack can also be used as a notification sink.

When Slack notifications are enabled for a workflow, choose:

| Mode | Behavior |
| --- | --- |
| All results | Send a message when a run succeeds or fails. |
| Failures only | Send a message only when a run fails. |

Pull-request workflows can also send review reminders and merged pull request updates when that lifecycle notification is enabled.

Notification configuration is separate from Slack context. A workflow can notify Slack without reading Slack context, or read Slack context without sending run notifications.

## Safety boundaries

Workflow integration access follows these rules:

- integrations are read-only context unless a provider-specific write capability is explicitly implemented
- docs updates are written through the connected Git provider
- connected providers are used only when the workflow requests that capability
- workflow runs should record which connectors were used for audit and usage reporting
- disconnecting an integration stops future workflow access to that provider

## Troubleshooting

### Integration does not appear in workflow settings

Confirm the integration is connected, the workflow supports that integration family, and the deployment is eligible for workflows.

### Source repository does not appear

Confirm the repository was added in Git settings as a source repository, not only as a copy target. Copy targets are used for migration or backup checks and do not automatically become workflow context.

If the repository is self-hosted, confirm the instance URL is reachable from dubstack and the saved token can read the repository.

### Code-change trigger does not run

Confirm the source repository card shows the expected event support, the webhook is active when you expect reactive runs, and the change was merged through a pull request or merge request. Direct pushes can be inspected by scheduled or manual runs, but code-change workflow triggers are review-event driven.

### Slack channel is missing

Invite the app to the channel. Private channels only appear after the app has been added.

### Jira query returns no context

Check that the connected Jira account can see the issues returned by the JQL query. Keep the issue limit low while testing.

### Confluence query returns no context

Check that the connected Atlassian account can see the pages returned by the CQL query. Keep the page limit low while testing.

### Workflow runs without expected context

Open the run details and check the selected integrations, collected context count, and readiness messages. If the integration was disconnected or missing scopes, reconnect it from **Settings > Organization > Integrations**.

## Related

- [Configure workflow runs](/workflows/configuration)
- [Connector capabilities](/ai/connector-capabilities)
- [Integration permissions and scopes](/ai/integration-permissions)
- [Slack integration](/ai/integrations/slack)
- [Jira integration](/ai/integrations/jira)
- [Confluence integration](/ai/integrations/confluence)

---
title: "List drum kits"
openapi: "GET /kits"
method: "GET"
path: "/kits"
specName: "api-reference"
operationId: "listKits"
description: "Returns all available drum kits with rich metadata for visual preview rendering. Each kit includes brand, year, genre tags, accent color, and detailed sound information."
---

Returns all available drum kits with rich metadata for visual preview rendering. Each kit includes brand, year, genre tags, accent color, and detailed sound information.

> `GET /kits`

### Responses
- `200` Array of available drum kits with rich metadata.

<PlaygroundEmbed specName="api-reference" method="GET" path="/kits" operationId="listKits" />

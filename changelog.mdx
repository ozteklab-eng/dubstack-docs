---
title: Changelog
description: Track product documentation updates.
icon: history
rss: true
---

Follow this changelog for notable updates to the dubstack product documentation.

<Update label="Git settings states clarified" description="Explained active sources, authoring reconnects, source repositories, copy targets, and webhook state labels." tags={["deployment", "workflows"]}>
  Git settings docs now clarify how multiple provider connections can coexist without changing the live deployment source.
</Update>

<Update label="Forgejo setup clarified" description="Separated Forgejo repository-copy targets, workflow source repositories, and environment-enabled deployment-source mode." tags={["deployment", "workflows"]}>
  Git settings now documents Forgejo as safe workflow source context and repository-copy target setup by default, with active deployment-source mode shown only when it is enabled for the workspace.
</Update>

<Update label="Repository copy workflows" description="Documented one-time repository copy workflows, copy targets, run results, and credit usage." tags={["workflows", "deployment"]}>
  Repository copy workflows can copy the active docs source into a configured provider target for backup, provider testing, or migration rehearsal without changing the live deployment source.
</Update>

<Update label="Mounted docs MCP validation" description="Validated public MCP discovery, search, docs filesystem access, and feedback submission through a /docs route mount." tags={["mcp", "deployment"]}>
  Public MCP endpoints now work through host-based docs sites and path-mounted custom domains when the proxy forwards the MCP, discovery, and feedback paths.
</Update>

<Update label="Custom-domain verification docs" description="Documented verification TXT records, traffic CNAME setup, TLS readiness, and /docs route-mount boundaries." tags={["deployment", "docs"]}>
  The custom-domain guide now explains why host-based domains use separate hostname ownership, certificate validation, and traffic routing records before a domain is production-ready.
</Update>

<Update label="Public docs expanded" description="Expanded the customer-facing product docs with clearer AI, Theme Studio, API, deployment, and configuration guidance." tags={["docs"]}>
  The docs now separate public product guidance from internal architecture notes and use the canonical Git-backed product docs repository as the source of truth.
</Update>

<Update label="API reference added" description="Added the docs runtime OpenAPI reference, API playground guidance, MCP discovery endpoints, and assistant request examples." tags={["api", "docs"]}>
  The API reference is generated from the product docs OpenAPI spec and is published as part of the public docs site.
</Update>

<Update label="MCP documentation added" description="Documented the public MCP retrieval tools, optional agent feedback, discovery paths, skill file, and current authoring boundaries." tags={["mcp", "ai"]}>
  Public MCP access is scoped to published docs content and remains separate from authenticated dashboard authoring workflows.
</Update>

<Update label="Initial product docs scaffold" description="Created the canonical dubstack product documentation repository structure." tags={["docs"]}>
  The first version defined the customer-facing docs information architecture and separated it from starter templates and internal operations notes.
</Update>

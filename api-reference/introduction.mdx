---
title: API reference
description: Publish interactive API documentation from OpenAPI specs and MDX endpoint pages.
icon: book
---

dubstack can render API documentation from OpenAPI specs, manually authored MDX endpoint pages, or a mix of both.

Use this section for two related tasks:

- learning how to document your own product APIs inside a dubstack docs site
- integrating trusted automation with the dubstack Admin API

## Supported API documentation paths

| Path | Use it when |
| --- | --- |
| OpenAPI-generated pages | You already have an OpenAPI 3.x JSON spec and want dubstack to generate endpoint pages from it. |
| Manual MDX endpoint pages | You want full control over page copy while still attaching each page to an API operation. |
| Embedded playgrounds | You want to place a runnable endpoint example inside a guide or tutorial. |
| dubstack Admin API | You want to trigger deployments, create branch previews, run workflows, retrieve published content, or export analytics from trusted server-side automation. |

## Source of truth

Keep API specs in the same Git repository as the rest of your docs.

The canonical location for the main spec is:

```text
api-reference/openapi.json
```

You can also store named specs under:

```text
api-reference/openapi/payments.json
api-reference/openapi/admin.json
```

After the repo syncs, dubstack stores the spec with the published docs artifacts and uses it to render API reference pages and playgrounds.

## What to read next

- [OpenAPI setup](/api-reference/openapi-setup): add a spec and generate endpoint pages.
- [Manage API specifications](/api-reference/spec-management): upload, refresh, and control endpoint visibility from the dashboard.
- [Playground authentication](/api-reference/playground-authentication): configure how users try requests from the docs site.
- [Workflow API triggers](/api-reference/workflow-triggers): queue enabled workflows from trusted backend automation.
- [Analytics API exports](/api-reference/analytics-exports): export usage and feedback analytics from trusted backend systems.
- [Published content API](/api-reference/published-content-api): search a deployment's published docs and retrieve complete page content.
- dubstack Admin API: open a generated endpoint under this section to inspect its request, response, scope, and executable code examples.

<Callout type="info">
  Admin API keys are server-side secrets. Keep them out of browser code, public repositories, client logs, and request examples.
</Callout>
